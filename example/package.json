{
  "name": "example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm generate:airtable && vite",
    "build": "pnpm generate:airtable && tsc --noEmit && vite build",
    "preview": "vite preview",
    "generate:airtable": "../bin/dev app0JmQpZFn7YCI4C -z -o ./src/airtable-schemas.ts"
  },
  "dependencies": {
    "airtable": "^0.11.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.9",
    "@vitejs/plugin-react-swc": "^3.0.0",
    "airtable-typegen": "^0.2.2",
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  }
}
